# frozen_string_literal: true

RSpec.describe "claude:install generator" do
  it "creates .claude directory structure" do
    # Test would go here when generator_spec gem is added
    skip "Add generator_spec gem for testing"
  end
end
